name: Jest Tests
on: [push]
jobs:
  unit-test:
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.12
      - run: npm ci
      - run: npm test
    runs-on: ubuntu-latest
