version: 2
jobs:
  build-and-deploy:
    docker:
      - image: circleci/node:10

    steps:
      - checkout
      - run:
          name: test
          command: npm run test
      - run:
          name: code-coverage
          command: bash <(curl -s https://codecov.io/bash)
