h2.typo__h2#select-search Single select with search
p.typo__p The selected value is referencing the same object as the previous example, to show that dynamic preselection works.
.grid__row
  .grid__column.grid__unit--md-5
    label.typo__label Simple select / dropdown
    multiselect(
      v-model="valueSearch",
      :options="source",
      :searchable="true",
      :custom-label="nameWithLang"
      placeholder="Select one",
      label="name",
      track-by="name"
    )
    pre.language-json
      code.
        {{ value  }}

  .grid__column.grid__unit--md-7
    label.typo__label Code sample
    pre.language-jade
      code.
        multiselect(
          v-model="valueSearch",
          :options="source",
          :searchable="true",
          :custom-label="nameWithLang"
          placeholder="Select one",
          label="name",
          track-by="name"
        )
    pre.language-javascript
      code.
        nameWithLang ({ name, language }) {
          return `${name} — [${language}]`
        }
